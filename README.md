# Список исправленных ошибок в соответствии со стандартом Epic Games C++ Coding Style
## Вариант 1
### Tasks1
Для выполнения работы были сгенерированы три варианта программы "Генератор паролей" с использованием трёх различных чатов GPT. Каждому чату был отправлен одинаковый промт:

Напишите генератор паролей. Составьте три уровня сложности генерации паролей (вместе
с их длиной) и спрашивайте у пользователя, какой уровень сложности ему нужен. Проявите свою
изобретательность: надёжные пароли должны состоять из сочетания строчных букв, прописных
букв, цифр и символов. Пароли должны генерироваться случайным образом каждый раз, когда
пользователь запрашивает новый пароль.

Напиши программу на C++.

#### Task1.1
1. Глобальные директивы
   - Убрано using namespace std в глобальной области видимости, добавлен префикс std:: ко всем объектам стандартной библиотеки
2. Структура проекта
   - Программа разделена на модули согласно стандарту Epic Games
   - Создан заголовочный файл Header.h с прототипами методов класса
   - Реализация методов вынесена в отдельный файл Source.cpp
   - В основном файле Task1.cpp оставлена только функция main
   - Получение длины пароля вынесено из main в отдельный статический метод класса (GetLengthByLevel)
3. Именование классов и методов
   - Методы переименованы в PascalCase
   - Примеры: setCharacterSets → SetCharacterSets, generate → Generate, levelDescription → GetLevelDescription, randomChar → GetRandomChar, charVectorToString → VectorToString
4. Именование переменных
   - Все переменные переименованы в PascalCase (с заглавной буквы)
   - Имена переменных сделаны информативными
   - Примеры: rng → RandomGenerator, charPool → CharacterPool, result → Result, remaining → Remaining
5. Инициализация переменных
   - Все переменные инициализированы при объявлении
6. Генерация случайных чисел
   - Заменён rand() на современный <random> с std::mt19937
   - Использование std::uniform_int_distribution для равномерного распределения
7. Форматирование кода
   - Фигурные скобки открываются в новой строке для всех управляющих конструкций
   - Добавлены фигурные скобки для однострочного if
8. Проверка ввода
   - Добавлена функция ClearInput() для очистки потока ввода
   - Добавлена функция GetValidLevel() для получения корректного уровня
   - Добавлена проверка на некорректный ввод
9. Локализация
   - Добавлена поддержка русского языка: setlocale(LC_ALL, "Russian");
10. Инкремент/декремент
    - Заменён постфиксный дикремент на префиксный: Remaining-- → --Remaining
11. Комментарии
    - Все функции прокомментированы (что делают)
    - Комментарии начинаются с заглавной буквы
    - Добавлен пробел после // в комментариях
    - Комментарии заканчиваются точкой
    - Коментарии приведены к нормальному виду, сделаны информативными
12. Логика генерации пароля
    - Для корректной генерации пароля определённого уровня добавлена проверка, какие классы входят в набор символов (CharacterPool)
    - Добавляются только символы их доступных классов
    - Исправлена ошибка, при которой для лёгкого уровня добавлялись цифры и символы
   
#### Task1.2
1. Структура проекта
   - Программа разделена на модули согласно стандарту Epic Games
   - Создан заголовочный файл Header.h с прототипами методов класса
   - Реализация методов вынесена в отдельный файл Source.cpp
   - В основном файле Task1.2.cpp оставлена только функция main
2. Инкапсуляция
   - Функции генерации пароля объединены в класс PasswordGenerator
   - Генератор случайных чисел сделан приватным полем класса
3. Именование функций
   - Функции переименованы в PascalCase (с заглавной буквы)
   - Примеры: getRandomInt → GetRandomInt, generatePassword → GeneratePassword
4. Именование параметров функций
   - Параметры переименованы в PascalCase (с заглавной буквы)
   - Примеры: min → Min, max → Max, length → Length, chars → CharacterSet, rng → RandomGenerator
5. Именование локальных переменных
   - Все локальные переменные переименованы в PascalCase (с заглавной буквы)
   - Имена локальных переменных сделаны информативными
   - Примеры: lowercase → Lowercase, uppercase → Uppercase, digits → Digits, symbols → Symbols, all_chars → AllChars, password_length → PasswordLength, charset → CharacterSet, password → Password, choice → Level
6. Инициализация локальных переменных
   - Все локальные переменные инициализированы при объявлении
7. Форматирование кода
   - Фигурные скобки открываются в новой строке для всех управляющих конструкций
   - Добавлены фигурные скобки для  веток case и default оператора switch
   - Добавлен оператор break в ветку default оператора switch для предотвращения сквозного выполнения
8. Проверка ввода
   - Добавлена функция ClearInput() для очистки потока ввода
   - Добавлена функция GetValidLevel() для получения корректного уровня
   - Добавлен цикл для повторного ввода при неверном выборе
9. Локализация
   - Добавлена поддержка русского языка: setlocale(LC_ALL, "Russian");
10. Комментарии
    - Все функции прокомментированы
    - Комментарии начинаются с заглавной буквы
    - Комментарии заканчиваются точкой
    - Коментарии приведены к нормальному виду, сделаны информативными

#### Task1.3
1. Структура проекта
   - Программа разделена на модули согласно стандарту Epic Games
   - Создан заголовочный файл Header.h с прототипами методов класса
   - Реализация методов вынесена в отдельный файл Source.cpp
   - В основном файле Task1.3.cpp оставлена только функция main
2. Инкапсуляция
   - Создан класс PasswordGenerator, объединяющий функциональность генерации паролей
   - Наборы символов вынесены в статические константы класса
   - Логика проверки наличия всех типов символов вынесена в отдельный приватный метод HasAllCharTypes
3. Именование классов и методов
   - Класс назван в PascalCase (с заглавной буквы): PasswordGenerator
   - Методы переименованы в PascalCase (с заглавной буквы)
   - Пример: generatePassword → GeneratePassword
4. Именование параметров функций
   - Параметры переименованы в PascalCase (с заглавной буквы)
   - Примеры: length → Length, useUpper → UseUpper, useLower → UseLower
5. Именование переменных
   - Все переменные переименованы в PascalCase (с заглавной буквы)
   - Имена переменных сделаны информативными
   - Примеры: chars → CharacterSet, password → Password, choice → Level
6. Инициализация переменных
   - Все переменные инициализированы при объявлении
   - Пример: int Length = 0;, std::string Password = "";
7. Форматирование кода
   - Фигурные скобки открываются в новой строке для всех управляющих конструкций
   - Добавлены фигурные скобки для однострочных if, for, switch
   - Добавлены фигурные скобки для  веток case и default оператора switch
8. Проверка ввода
   - Добавлена функция ClearInput() для очистки потока ввода
   - Добавлена функция GetValidLevel() для получения корректного уровня сложности
   - Добавлен цикл для повторного ввода при неверном выборе
9. Локализация
   - Добавлена поддержка русского языка: setlocale(LC_ALL, "Russian");
   - Сообщение об ошибке "Please select at least one character type." заменено на "Ошибка: не выбран ни один тип символов."
10. Комментарии
    - Все функции прокомментированы (что делают)
    - Комментарии заканчиваются точкой
    - Комментарии приведены к нормальному виду, сделаны информативными
11. Генерация случайных чисел
    - Инициализация генератора случайных чисел вынесена в конструктор класса PasswordGenerator
    - Использование std::srand() и std::rand() с static_cast для явного преобразования типов
12. Безопасность
    - Добавлен static_cast<unsigned char> при вызове функций std::isupper, std::islower, std::isdigit для безопасного преобразования символов
13. Логика генерации пароля
    - Проверка на пустой набор символов вынесена в начало метода
    - Дополнительная проверка для сложных паролей (уровень 3) реализована через рекурсивный вызов метода GeneratePassword
    - Метод HasAllCharTypes проверяет наличие всех требуемых типов символов

#### Интересные выводы:
- В Task1.1 использовался современный <random>, остальные - устревший rand()
- В Task1.1 чат сразу реализовал полноценный класс, остальные чаты использовали простые функции
- Ни один из вариантов кода изначально не соответствовал стандарту Epic Games
- Во всех вариантах кода требовалось исправление и добавление комментариев
- Ни один из чатов не предусмотрел корректную обработку ввода пользователя
- Чаты использовали русский язык в сообщениях, но поддержка русского языка не была подключёна в коде
- Task1.2 и Task1.3 изначально не использовали using namespace std в глобальной области видимости, Task1.1 - использовал (нарушение стандарта Epic Games)
- Все варианты программного кода требовали форматирования, каждый чат открывает фигурную скобку в строке с условием цикла
- Все чаты пишут программный код в одном файле, поэтому для каждого программного кода требовалось разделение на модули

### Tasks2
#### Task2.1


#### Task2.2
1. Глобальные директивы
   - Убрано using namespace std в глобальной области видимости, добавлен префикс std:: ко всем объектам стандартной библиотеки
2. Структура проекта
   - Программа разделена на модули согласно стандарту Epic Games
   - Создан заголовочный файл Header.h с прототипами методов класса
   - Реализация методов вынесена в отдельный файл Source.cpp
   - В основном файле Task1.cpp оставлена только функция main
   - Чтение из файла и проверка корректности выражения вынесены в отдельный метод класса (LoadFromFile)
   - Установка значения переменной x вынесена в отдельный метод класса (SetXValue)
3. Инкапсуляция
   - Структура Node преобразована в класс с приватными полями
   - Добавлены конструкторы для различных типов узлов
   - Добавлены геттеры для доступа к полям класса Node
   - Добавлены сеттеры для преобразования дерева
   - Создан класс ExpressionTree, содержащий методы для работы с деревом выражения
   - Рекурсивные методы класса ExpressionTree для работы с деревом выражения сделаны приватными
   - В класс ExpressionTree добавлены публичные методы для вызова приватных рекурсивных
4. Именование классов и методов
   - Классы названы в PascalCase (с заглавной буквы): Node, ExpressionTree
   - Методы переименованы в PascalCase (с заглавной буквы)
   - Примеры: skipSpaces → SkipSpaces, parseFactor → ParseFactor, eval → Evaluate, printTree → PrintTree
5. Именование параметров функций
   - Параметры переименованы в PascalCase (с заглавной буквы)
   - Имена параметров сделаны информативными
   - Пример: node → Current, pos → Position, expr → Expression, val → Value, op → Operator
   - Имя параметра в методе TransformNode изменено с Node на Current, чтобы избежать конфликта с именем класса Node
6. Именование переменных
   - Все переменные переименованы в PascalCase (с заглавной буквы)
   - Имена переменных сделаны информативными
   - Примеры: pos →Position, val → Value, op → Operator, left → Left, right → Right
7. Форматирование кода
   - Фигурные скобки открываются в новой строке для всех управляющих конструкций
   - Добавлены фигурные скобки для однострочных if, for, while
8. Проверка ввода
   - Добавлена функция ClearInput() для очистки потока ввода
   - Добавлена функция GetXValue() для получения корректного значения x
9. Локализация
   - Добавлена поддержка русского языка: setlocale(LC_ALL, "Russian");
10. Комментарии
    - Все функции прокомментированы (что делают)
    - Комментарии начинаются с заглавной буквы
    - Комментарии заканчиваются точкой
    - Коментарии приведены к нормальному виду, сделаны информативными
11. Инкремент/декремент
    - Заменён постфиксный иНкремент на префиксный: pos++ → ++Position
12. Пользовательский интерфейс
    - Сообщения для пользователя написаны на русском языке
    - Добавлено сообщение о завершении работы программы для удобства пользователя
